extends layout
include mixins/_storeCard


block content
  .inner
    h2 #{title}
    //- logs our stores
    //- pre #{h.dump(stores)}
    .stores
      each store in stores
        +storeCard(store)

